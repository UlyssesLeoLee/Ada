[toolchain]
# D-04 Bevy 0.14 互換性のため Rust 1.98+ を要求（v0.1.0 升版, per 523afda）
# Rust 1.98.0 (本環境 host toolchain, 2026-08-18 stable) でビルド検証済み
channel = "stable"
components = ["rustfmt", "clippy", "rust-src"]
profile = "minimal"
