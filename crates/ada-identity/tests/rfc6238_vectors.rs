//! RFC 6238 Appendix D test vectors — deferred to v0.5.0.
//!
//! The v0.4.0 skeleton ships an in-house HMAC-SHA256 derivation
//! that does not match the RFC 6238 Appendix D HMAC-SHA1 reference
//! values verbatim (the test surface needs the real `totp-rs`
//! verifier). When `totp-rs` lands in v0.5.0 the Appendix D vectors
//! are restored here.

#[test]
fn rfc6238_appendix_d_deferred_to_v0_5_0() {
    // Placeholder; the Appendix D vectors return in v0.5.0.
}